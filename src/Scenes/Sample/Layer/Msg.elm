module Scenes.Sample.Layer.Msg exposing (Init)

{-| Msg module for layer.

You may want to put this in `LayerBase` module.

If you don't need communication between layers, you can remove this module.

@docs Init

-}


{-| Layer init type
-}
type alias Init =
    {}
